
AMILab Engine:

keep only the scripting language and the wrapping system from amilab

1. Create a CMakeLists.txt

2. Copy only useful files/directories/libraries

dependencies:

flex/bison
python
C++
gccxml

boost? : avoid boost

compilation CMake

configure cpack ...

licence FreeBSD???

multithreading ???

AMILabEngineConfig.h